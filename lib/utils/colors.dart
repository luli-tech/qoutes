import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.redAccent;
  static const secondary = Colors.blueAccent;
  static const background = Color.fromARGB(255, 12, 6, 39);
  static const text = Colors.white;
  static const appBar = Color.fromARGB(255, 12, 6, 39);
}
